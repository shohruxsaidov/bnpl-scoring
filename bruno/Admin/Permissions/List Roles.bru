meta {
  name: List Roles
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/permissions/roles
  body: none
  auth: none
}

params:query {
  platform:
}

docs {
  Requires manage_roles permission. platform: "merchant" | "admin" (optional filter).
}
