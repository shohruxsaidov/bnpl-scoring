meta {
  name: Refresh
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/auth/client/refresh
  body: none
  auth: none
}

docs {
  Reads session_id cookie. Issues a new access_token cookie (15 min TTL).
}
