meta {
  name: Me
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/auth/client/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

script:pre-request {
  const cookie = req.getHeaders()
  console.log(cookie)
}

docs {
  Requires Authorization: Bearer <accessToken> header. Returns the authenticated user's profile.
}
